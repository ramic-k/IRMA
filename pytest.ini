[pytest]
testpaths = tests
# import the checkout, not an installed irma, under a bare `pytest`
pythonpath = .
addopts = -q --tb=short
markers =
    mlip_real: needs a real MLIP potential package + checkpoint download; gated on IRMA_MLIP_REAL_TESTS=1, never in CI
