# Documentation build dependencies (mkdocs --strict, the `pages` CI job).
# Pinned below the next major so an upstream mkdocs-material release cannot
# silently break the strict docs build on an unrelated commit. Raise the cap
# after rebuilding the docs against a new major.
mkdocs-material>=9.7.7,<10
