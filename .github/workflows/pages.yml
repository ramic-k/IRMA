name: Docs

# GitLab `pages` (default branch only) -> GitHub Pages. Same strict build:
# mkdocs --strict fails on broken links, missing nav entries, or a stale
# version/CHANGELOG reference, so nothing publishes unless the docs are clean.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.12", cache: pip}
      - run: pip install -r requirements-docs.txt
      - run: python -m mkdocs build --strict -d public
      - uses: actions/upload-pages-artifact@v3
        with: {path: public}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
